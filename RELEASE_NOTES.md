## v9.2.0 · 多钱包并行下单 + 排除品类 + 直接市价清仓

这一版有两个新开关（都默认关，不动配置升级上来行为不变），一处会让多钱包用户明显感到变化的提速，以及一个重启相关的修复。

---

### 一、多钱包不再排队下单

以前所有钱包共用一条下单流水线，一个跑完才轮到下一个。每个钱包要把候选市场逐个走一遍，市场多、代理慢的时候单个钱包就得跑八到十一分钟；七个钱包排下来，一轮六十多分钟，也就是说任何一个钱包挂完这次单，要等一个多小时才轮到它下一次。中间它既不补新单，也不撤该撤的单。

有位用户的实盘日志里，某个钱包两次挂单之间隔了 67 分钟。他看到的现象是"成交卖出之后就不挂了"，其实是排队没轮到。

现在各钱包同时下单，一轮的时间等于最慢的那个钱包，不再是所有钱包相加。钱包越多，差别越大。

**这意味着挂单和撤单都会比以前频繁得多。**单个钱包的下单逻辑一个字没改，挑哪个价、挂几份、挂哪个市场，判断完全和以前一样，只是轮到它的次数多了。如果你之前是按"一小时才动一次"的节奏观察程序，升级后请重新看一眼实际频率。

### 二、排除品类

配置页的品类表格多了一列「排除」。勾中的品类一票否决：只要市场带这个标签就不做，哪怕它同时带着你勾了「做市」的标签。

为什么需要这个：官方品类是互相重叠的。同一位用户勾了「政治」、没勾「选举」，结果程序还是在一个巴西州长选举的市场上挂了单——那个市场在 Polymarket 那边同时挂在政治和选举两个分类下，而「做市」这一列的判断是"命中任一个勾中的品类就做"，政治命中了就放行。翻他的候选池，带选举标签的 54 个市场里有 44 个同时带政治标签，占八成，靠不勾「选举」根本挡不住。

这类重叠不止选举一处，地缘政治和政治、人工智能和科技、电竞和体育都有大量重叠。要拒绝某个品类，勾「排除」。

排除优先于做市，两列在界面上互斥。「其他/未分类」那一行没有这一列——它本来就不带任何标签，没有可排除的东西，仍归「做市」列管。

**勾上之后会撤单。**被排除的市场下一轮就跌出候选，程序会撤掉这些市场上还挂着的买单（正在冷却的市场除外）。已有持仓不受影响，照常按离场规则卖出。

### 三、止盈方式多一个「直接市价清仓」

配置页「止盈方式」原本两个选项，现在多一个：不论盈亏，只要有持仓就立刻市价卖出，不挂单等价差、不等回本。

它跳过平时的两段式判断，成本价算不出来也照卖（正常模式下遇到这种情况会跳过并报「裸奔」警告）。盘口没有买盘时卖不出去，这时不会记一笔假成交，而是发一条警告状态行，下一轮再试。

默认仍是「挂卖一做 maker」，想用才选。

### 四、修复：重启引擎会留下停不掉的后台扫描

以前点「停止引擎」时，如果扫描线程正好在做市场发现（这一步要跑十几到二十几分钟），程序等三十秒等不到就放弃、把线程丢开不管了；紧接着「启动引擎」又把停止信号清掉，那条旧线程醒来一看没人让它停，就继续跑下去，直到整个程序被关闭为止。

每重启一次引擎，就多留一条这样的线程。两条线程同时下单会在同一个市场重复挂单，实际敞口翻倍，还会互相撤掉对方刚挂的单——日志里那批"already canceled"就是这么来的。

现在停止和启动引擎都会作废上一代线程，旧线程在下一个检查点会自己退出；市场发现过程本身也能被中途叫停，不用再干等它跑完。

---

### 升级说明

不需要改任何配置，两个新开关默认都是关的。

多钱包用户请留意第一条：下单频率会明显上升，这是并行带来的正常结果，不是程序出问题。
