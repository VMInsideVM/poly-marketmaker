## v3.1.0 · 品类白名单勾选 + 单份奖励阈值可关 + 策略说明翻新

上一个正式版本是 v2.4.0，这一版把 v3.0.0、v3.1.0 两批改动合在一起发布。

### 新增：做市品类改为「白名单勾选」

以前排除哪些品类是写死在代码里的（体育 / 电竞 / 天气三类），现在改成配置页里自己勾。所有品类以复选框列出，勾上的才做，没勾的一律跳过；另带一个「其他 / 未归类」兜底项，用来收那些不属于任何已知品类的市场。品类清单会按当前市场实时统计各品类的数量，勾选时能看到每类大概有多少个可做。

### 新增：单份奖励阈值可以整体关掉

「单份奖励阈值」这道筛选（单份奖励 = 市场每日 LP 奖励 ÷ 最低份数，低于阈值就不做）现在多了一个总开关。不想按这个门槛筛，直接关掉，所有市场都放行；想用就打开，再按份额档位分别配阈值。默认仍是开启，行为不变。

### 说明与文档翻新

- 配置页给「单份奖励」和「累计厚度 / 风险系数」都加了一行小字，说明每个数是怎么算出来的。
- 使用说明页（帮助）和 README 重写了策略这一段，把单份奖励、累计厚度、风险系数三个概念用例子讲清楚。
- 统一术语：界面里之前混用的「累加厚度」全部改成「累计厚度」。

### 升级与兼容

静默安装、自动重启；本地数据库和已有模板原样沿用。所有新增字段都有默认值，更新后行为不变，想用新功能到「配置 → 策略参数」里开启即可。
