## v2.1.0 · 成本价保本离场 + UMA 结算自动撤买单 + 每钱包代理

本次新增三项功能，老用户什么都不用改、照常使用即可，默认行为和上个版本一致。

### 新增：按成本价保本离场

当持仓成本不高于买一价（已处于保本 / 盈利区）时，离场方式多了一个「按成本价」选项。选它时卖单挂在成本价：能立即成交的部分按更高的买一价成交，剩余停在成本价当保本单，绝不卖穿成本。

在「配置 → 成本≤买一时离场方式」里切换；默认仍是「挂卖一」，不动就和以前一样。

### 新增：UMA 结算提交即撤买单（自动保护）

一旦有人在 UMA 上对某个市场提交了结算（该市场随时会定价为 0 或 1），程序会立刻撤掉这个市场里你所有的挂买单，并在本轮不再挂新买单，避免在结算前一刻被成交、接到将要归零的份额。持仓和卖单不受影响，仍按正常离场逻辑卖出。

此保护默认开启、无需配置。

### 新增：每个钱包单独配 IP 代理

可以给每个钱包单独填一个代理，填好后这个钱包的所有网络活动都从对应的代理 IP 出口，实现按钱包的 IP 隔离。

- 在「配置 → 钱包管理」新增钱包时填「代理」，或在钱包列表点「代理」按钮编辑。
- 格式：`host:port` 或 `host:port:用户名:密码`。
- 留空 = 直连（和以前一样）。仅支持 HTTP / HTTPS 代理。
- 代理连不上时，该钱包本轮操作会报错跳过，绝不改用真实 IP 直连。
- 代理改动在下次启动该钱包引擎时生效。

### 升级与兼容

静默安装、自动重启；本地数据库自动迁移，旧设置原样沿用，建议所有用户更新。
