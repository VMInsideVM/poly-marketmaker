## v8.0.0 · 奖励掉了会自动撤单

这版改了做市行为，所以是个主版本。以前一个市场把每日奖励砍下来，程序最长 4 小时都发现不了，资金一直在那儿白挂着。现在挂单检查会顺带盯着奖励。

### 奖励跌破门槛就撤单

在挂单的市场，每 5 秒跟着挂单检查一起看一眼实时奖励。低于配置页里那个「最低奖励金额 (USD)」就立刻把这个市场的买单撤掉，不重挂。持仓不动，还是照原来的离场规则卖。

历史页的动作列能看到「奖励下降撤单」，理由里写着当时的实时奖励和门槛，比如 `实时每日奖励 $5.00 < 门槛 $100.00`。筛选框里也能单独把这一类挑出来看。

**撤掉之后最长 4 小时不会重挂。** 撤单的同时程序把新的奖励值写回候选池，下一轮下单就不会再挑中它；等下次市场发现（默认 4 小时一轮）重新拉一遍奖励，那时候要是涨回来了，市场自然又回到可下单的名单里。奖励降了又马上升回来的情况，就得等这一轮。

两件事要留意。

⚠️ **如果你动过配置页的「奖励参数缓存 (秒)」**，得手动把它改成 0，实时复查才会生效。这一项的默认值这版从 600 改成了 0，但只对从没动过它的人生效——你自己填过的值存在库里，升级不会覆盖。

⚠️ **这会连带影响低余额清仓的顺序。** 低余额清仓的第一档是「低奖励市场优先卖」，判断依据就是候选池里的奖励值。奖励降下来的市场现在会更早被市价卖掉（这一档本来就允许低于成本卖）。

### 挂单多的钱包，复查快了一个量级

挂单复查以前是一个单一个单串行取数据，几十个在挂单就得跑几十秒；因为它排在监控的最后一步，这几十秒会把下一轮的成交检测和止损一起往后推。

现在取数据改成 6 路并发。60 个在挂单的钱包，一轮从半分钟量级降到几秒，止损和成交检测的实际间隔跟着缩短。挂单少的钱包本来就快，感觉不出区别。

顺带一个副作用：黑名单和策略模板现在是每轮读一次快照，跑到一半改了配置、或者把某个市场拉黑了，要等下一轮（约 5 秒）才生效。

### 升级说明

直接更新就行，钱包数据和配置都不用动。只有上面那条「奖励参数缓存」需要你自己去看一眼——从没动过就不用管。
