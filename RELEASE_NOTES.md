## v1.0.16 · 界面全面重做 + 多项稳定性修复

### 界面

- **改为左侧边栏 + 7 个页面**:仪表盘 / 市场发现 / 挂单与持仓 / 历史 / 监控 / 配置 / 黑名单。信息分门别类，不再挤在一页。
- **支持深色 / 浅色主题切换。** 侧边栏底部一个按钮即可切换，选择会被记住。
- **新增「市场发现」页。** 每个合格市场直接显示最低份数、单份奖励（含是否达到取档阈值）、奖励范围、盘口价差；点「展开」还能实时预演该市场的多档挂单梯队 —— 每一档的有效价格、订单厚度、累加厚度、对应份额，并把「这一档为什么不挂」也一并列出，做市逻辑一目了然。
- **挂单与持仓合并到一页**；历史页汇总全部引擎动作（挂买 / 止盈止损 / 撤改）及每笔卖单的成本依据；监控页只保留实时状态快照。

### 修复

- **止盈 / 止损挂单若被交易所拒单，不再静默失败。** 此前拒单会被吞掉、监控页什么都不显示，持仓悄悄裸奔；现在会显示醒目的「⚠️ 失败·裸奔」提示，让未受保护的持仓立刻可见。
- **修复扫描在个别市场字段缺失时可能整轮中断的问题。**
- **修复若干隐患**:同价重复买单未撤掉、持仓列表可能被服务端分页截断而漏掉离场、撤单失败无任何记录。
- **退出登录时清除内存中的解密密钥**;启动时端口不可用会给出明确报错，而不是崩在晦涩的系统错误上。

升级为静默安装并自动重启，数据与配置不受影响，建议所有用户更新。

> macOS（Apple 芯片）版 `.dmg` 随本次发布一同提供；macOS 端暂不支持自动更新，请手动到 Releases 下载新版。
