## v7.2.0 · 批量导入钱包，钱包名字到处都能看见

这版只加了三个界面上的功能，做市、下单、离场的行为和 v7.1.0 完全一样。

### 钱包可以一次导一批了

配置页「钱包管理」下面多了一个「批量导入」，点开是个文本框，一行一个钱包：

```
私钥,代理,备注
```

代理和备注可以留空，只填私钥也行。中间那个字段空着就是不走代理：

```
0xabc…,1.2.3.4:8080:账户:密码,主号
0xdef…,,小号2
0x123…
```

逗号用全角的「，」也认，中文输入法打出来不用改。

点「开始导入」之后文本框会立刻清空，私钥不会一直明晃晃留在页面上。

**导入要花点时间。** 每个钱包都要试一遍代理能不能连通、再查一遍是哪种账户类型，一个大概十秒上下，十几个就得跑几分钟。跑的时候上面有进度，导到第几个看得见。这期间可以切到别的页面看，回来还接着显示。

**存款钱包地址不再逐个弹窗让你确认了。** 批量导的时候一律自动识别，导完在结果表里列出来，你对着 polymarket.com 上的地址扫一眼就行。万一有对不上的（智能合约钱包、邮箱钱包容易这样），把那个删掉，用上面单个导入的框手填正确地址补一遍。

**一行出错不影响别的行。** 私钥打错了、代理连不通、这个钱包早就导过了，结果表里那一行会标出原因，其余的照常导进去。

### 每个钱包上次干活是什么时候

仪表盘的钱包表多了一列「上次活跃」，显示这个钱包最近一次成功挂出买单或者有成交是多久以前，比如「3 分钟前」「2 小时前」。鼠标停上去能看到具体时间。

哪个钱包卡住了、代理断了半天没动静，扫一眼就知道。从来没活跃过的显示「—」。

### 备注名现在到处都能看见

以前只有仪表盘、历史、实时监控这几个地方显示备注名，「挂单与持仓」页还是一串 `0x1a2b...9f3c`，看半天认不出是哪个号。

现在凡是显示钱包的地方都优先显示你起的名字，挂单表、持仓表、各种筛选下拉全都改了。没起名字的还是显示短地址，鼠标停上去能看到完整地址。

### 升级说明

直接更新就行，钱包数据不用动。「上次活跃」这一列从这次更新之后开始记，刚装好时都是「—」。
