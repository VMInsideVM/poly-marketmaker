## v9.0.0 · 断层分级改看整个买单簿；v1.0.15 的老策略回来了

**这一版会改变挂单行为，升级后你大概会看到挂单变少。** 这是有意的，原因见下面第一节。另外两个新功能默认都是关的，不动配置就不会生效。

---

### 一、断层分级现在看整个买单簿（行为改变）

有用户报了一个市场：买单簿是 21¢ 挂 236 份、19¢ 挂 120 份，再往下直接掉到 8¢。奖励区间是 19.5¢ 到 27.5¢，程序照样在 21¢ 挂了单。

查下来是分级的取值范围有问题。以前判断「断层」只看奖励区间**里面**的买档，而这个市场区间里只剩 21¢ 一档，算不出任何价差，于是被当成最平稳的「密盘」处理，规则1 那道厚度闸门根本没机会执行。19¢ 往下那个 11¢ 的深坑，全程没进入判断。

更要命的是，这不是这一个市场的巧合。奖励区间的宽度等于 max_spread 的两倍，所以按旧口径，**只要你的「买卖价差上限」配成 5 美分或更小，规则1 就永远不可能触发**，那道厚度闸门从配上去那天起就是死的。

现在改成看**整个买单簿**：分级和高位厚度都在全簿上算（包括奖励区间外的档，区间下方的深坑正是要防的东西），挂单价仍然只在奖励区间内选。上面那个市场现在会被正确判成「宽断层」，并因为上方厚度不够而整个不挂。

**你会感觉到的变化**：真实买单簿的低价区（1 到 8 美分那一带）通常比较稀疏，很容易冒出超过 10 美分的断层，所以大部分市场现在会归到规则1。这意味着：

- 挂单量会下降，因为以前漏判的危险市场现在被拦住了。
- **规则1 的两个参数（选档系数门槛、高位系数和门槛）从此是主力**，调参先调它们。规则2、规则3 的门槛虽然还能配，但触发机会不多。

---

### 二、规则2 和规则3 也可以设厚度闸门了（默认关）

以前只有规则1 有「高位系数和门槛」这道市场级闸门：断层上方到买一那一段的风险系数之和不够，整个市场就不挂。现在规则2、规则3 各有自己的一个，在档位模块里逐档配。

**默认都是 0，也就是不拦**，所以不动配置的话行为完全不变。

如果你把某一级的门槛从 0 调上去，那些不再合格的市场，它们上面已经挂着的买单会在下一轮被撤掉。这跟停用一个档位模块时的行为一致，属正常。

---

### 三、v1.0.15 的老策略回来了，可以随时切换（默认关）

v1.0.15 那套「找厚墙、挂在墙下面一档」的挂单策略，现在作为一个可选模式移植了回来，在「配置 → 挂单参数 → 挂单模式」里切换。**默认仍是现行的断层单档**，不动配置就用不到它。

它是按模板生效的，所以你可以让一部分钱包跑老策略、另一部分跑现行策略，跑几天后到「盈亏」页对比收益。

移植回来的只有**挑价**和**挂几份**这两件事，其余一律沿用现在的程序：选品筛选、品类白名单、预算与敞口封顶、离场止损、每钱包代理、盈亏台账和周报都不受影响。

切之前请看帮助页的「两种挂单模式 · 怎么选、怎么切」，那里有完整的操作步骤和 7 条注意点。最要紧的三条先摆在这里：

1. **默认阈值 2000 / 6000 可能对现在的盘口偏高。** 那是 v1.0.15 年代的数值。上面提到的那个市场，最厚的一档只有 236 份，按 2000 的阈值会被判「无档达到阈值」而整个不挂。**切之前先去「市场发现」页展开几个市场，看看预演表里盘口量是什么量级，再定阈值。** 阈值定得比盘口高，结果就是一单也挂不出去。
2. **老策略下档位模块只剩「档位值」和「启用」有用**，用来圈定做哪些份额档的市场；它的挂单份数、各项门槛、金额数值表全部不生效。所以切过去之后仍然要至少启用一个档位模块。
3. **「挂单份数」选「按美元上限」的话必须填金额**，留 0 等于一单都不挂。

老策略不是百分之百还原 v1.0.15：监控侧的悬崖复查、实时奖励复查、盘口价差复查仍然生效，它们是独立风控，跟用哪套定价无关。想要纯原版行为，把「悬崖探测」配成 0。

---

### 四、修了 7 个问题

**配置页清空数字输入框会让这个钱包停止交易（重要）**

策略参数里任何一个数字框，如果你把它清空再保存，这个钱包接下来每一轮下单都会失败，而界面上看不出任何异常，只有日志里有一行记录。这个问题不只影响新功能，「宽断层阈值」「单市场最大敞口」这些老参数一样中招。

现在清空的框会被当作「这一项不改」，保存后保持原值；后端也加了一层兜底。**（唯一的例外是「最长结算天数」，那一项留空的意思仍然是「不限」。）**

**其余六个**

- 老策略在「按美元上限 / 全额余额」份数模式下，历史记录里写的份数不是实际挂出去的份数（会写成未经预算封顶的理论值）。
- 历史页把老策略的跳过记录标成了「断层单档·判定不挂」，标签改成中性的「策略判定不挂」，具体哪套策略看「原因」列。
- 老策略在余额较小时，余额稍有变动就会把在挂买单全撤重挂，反复丢失队列位置。
- 老策略挂单成功时会把整个买单簿写进历史记录（几十档、上千字符），现在只记命中的那堵墙和实际挂的那一档。
- 悬崖否决时，预演页会对 0.1 美分盘错误地显示「厚墙（1 美分盘）」。
- 逐档挂量带小数而累计值取整，并排显示时相加对不上。

---

### 五、文档

README 和帮助页都补了「两种挂单模式怎么切、切之前要注意什么」，含操作步骤、7 条注意点、两个使用场景，以及一份「切了老策略却一单不挂」的排查对照表（按历史页「原因」列的文字对号入座）。

---

### 升级方式

程序启动时会自动检测新版本并提示，确认后下载、校验、静默安装并重启。也可以到 Releases 页手动下载安装包覆盖安装。运行数据（数据库、配置、私钥）不受影响。
