## v2.0.0 · 多档做市引擎 + 全新界面（首个 2.x 大版本）

2.0 把做市策略从「单边单档」整体换成了「多档梯队」，并配了一套全新的 7 屏界面。这次改动足够大，所以按版本号规范升为主版本 2.0.0；原先的 1.0.16 版本号偏小，已撤回，内容并入本版。

### 相对 1.0.16 的新修复

- **预算 / 敞口不够时，不再挂达不到奖励最低份数的残档。** 这种单赚不到奖励，还白占资金和敞口；现在会跳过该档，把剩余预算留给后面能凑满最低份数的档。
- **买单被部分吃掉后，若剩余份数已低于奖励最低要求，会自动撤掉重挂。** 此前这类残单会一直挂着，既不达标也占着仓位。

### 如果你从 1.0.15 或更早版本升级

这一版还包含此前的两大改动。

**全新做市引擎（多档梯队）**

- 一个市场的每一侧可同时挂多档，从买一往下取符合奖励区间、厚度达标的有效价位。
- 离场改为三段式:盈利挂卖一；水下按亏损幅度主动限损（够阈值就市价止损，否则挂可成交限价封住亏损，再不行先停在卖一等回弹）。
- 成本价只按真实成交逐笔重建，取不到就跳过并标「⚠️裸奔」，不按不靠谱的均价乱卖。

**全新界面（7 屏 + 主题）**

- 左侧边栏 + 7 个页面:仪表盘 / 市场发现 / 挂单与持仓 / 历史 / 监控 / 配置 / 黑名单。
- 深色 / 浅色主题随手切换，选择会被记住。
- 「市场发现」页能实时预演每个市场的多档挂单梯队:每档的有效价、订单厚度、份额，以及「这一档为什么不挂」。
- 止盈 / 止损被交易所拒单不再静默失败，会显示醒目的「⚠️ 失败·裸奔」提示。
- 修了若干隐患:扫描遇缺字段整轮中断、同价重复买单未撤、持仓列表被分页截断而漏掉离场、撤单失败无记录、退出登录残留内存密钥等。

### 升级与兼容

静默安装、自动重启，数据与配置不受影响，建议所有用户更新。

> macOS（Apple 芯片）版 `.dmg` 随本次发布一同提供；macOS 端暂不支持自动更新，请手动到 Releases 下载新版。
