## v4.0.0 · 下单方式收敛到唯一一种「网关式单档」

这是个主版本。之前「网关式单档」还只是个可选模式、默认仍走「多档做市」；这版把多档做市、单份奖励阈值、累积厚度这些老策略整个删掉，下单方式只剩「网关式单档」一种：按买单簿的断层分级选一档，挂最低份数一单。顺带修了两个会让人「明明该挂却挂不出单」的真 bug。

### 变了什么

**下单策略只剩网关式单档**

- 删掉「多档做市（laddering）」整套多档挂单。
- 删掉「单份奖励阈值（per_share）」筛选。
- 删掉「累积厚度 / 区间变量（tier_match_var）」。
- 现在每个市场只按断层规则选一档、挂最低份数一单。

**两个真 bug + 一处提速**

- **负风险市场的买单不再被拒签。** 很多天气类市场是负风险市场，之前这类市场的买单会被 Polymarket 以签名错误拒掉、一单都挂不出（长期被代理超时掩盖，切直连才暴露）。现在买单让客户端按 token 自查真实的负风险属性，跟卖单一致。
- **代理连接瞬时失败自动重试。** 烂的 / 轮换代理常有约三成连接超时，一轮下单要连续多个代理调用、任一失败就整轮放弃 → 几乎挂不出单。现在只对「连接建立阶段」的瞬时失败退避重试（仍走同一代理、绝不直连），把完成率拉回可用。
- **市场发现更快。** 发现阶段只查你实际勾选的品类，不再每轮固定查全部品类，从点击启动到「市场发现」出结果明显更快。

### 升级与兼容

- 配置页去掉了「挂单模式」下拉，以及 laddering / 单份奖励阈值 / 累积厚度相关的字段；现在只有网关式单档的参数（断层阈值、规则1/2/3 选档系数门槛、金额数值表、止盈方式、止损开关）。
- 已有模板里那些被删字段的旧值会被忽略，不影响使用；网关式单档的参数沿用默认，需要就到配置页调。
- 本地数据库、钱包、已有模板原样沿用，静默安装、自动重启。
