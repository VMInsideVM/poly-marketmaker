## v9.1.0 · 断层上限否决（默认关）

这一版加了一个新的风控开关，**默认是关的**，不动配置升级上来行为一模一样。另外修了两处显示问题，还纠正了帮助页里一条一直写反的排查建议。

---

### 一、新增「断层上限」（默认关）

配置页「悬崖探测」旁边多了一个「断层上限」。填了以后，整个买单簿的最大相邻价差一旦**超过**这个数（严格大于，恰好相等不算），这一侧就不挂了，也不再往下走分级选档那一套。

它跟已有的两个断层阈值不是一回事。「宽断层」「中断层」拿断层去**分级**，决定这个市场用哪一套选档门槛；断层上限拿断层去**一票否决**，判定排在分级之前。

下单前判一次，挂出去之后监控每 5 秒还会复查在挂的买单，超了就撤且不重挂（历史页记「撤单(断层过宽)」）。两处共用同一份计算，不会出现下单侧放行、监控侧撤单的来回空转。

**填之前请先看清楚这一点。** 断层量的是整个买单簿，而低价区那些零散挂单会把这个数撑得很大，**价格越高的市场撑得越狠**。举两个真实形状的簿：

- 一个健康的高价市场，买档 0.50 / 0.48 / 0.46 挨得很密，簿底有人挂了一笔 0.02。最大断层是 0.46 到 0.02 那一段，44¢。
- 一个危险的低价市场，买档只有 0.19 / 0.18，下面直接砸到 0.02。最大断层是 0.18 到 0.02，16¢。

密集的那个 44¢，真空的那个 16¢。所以拿一个上限一刀切，先被拦掉的反而是高价的健康市场。建议去「市场发现」页展开几个你实际在做的市场，看一眼「最大断层」的真实数值，照着定。

---

### 二、两条关于「悬崖探测」的澄清

这两点都不是这一版改的行为，是原本就那样、但一直没讲清楚的。

**这个数越大越宽松，越小越严格。** 它查的是「奖励区间下沿往下这一段里有没有买档」，段越宽就越容易找到人，也就越不容易判成悬崖。填 0.5 意味着只有紧贴下沿半美分内有档才算数，几乎所有市场都会被否掉。帮助页里原本写着「被悬崖否了就把悬崖探测调小或设 0」，方向是反的，已经改成「调大」并说明了原因。

**悬崖探测本身就是按深度否决。** 「下沿往下 2¢ 内一个档都没有」和「下沿到下面第一个买档的距离超过 2¢」是同一句话。想要「缺口超过 5 美分就不挂」，把悬崖探测填 5 就是了，不需要用上面那个新参数。新参数的区别在于它量的是全簿任意位置的最大缺口，而悬崖只量下沿紧邻的那一段。

---

### 三、修了两处显示问题

**跳过记录会整条丢失。** 一个市场被归级前的闸门否掉时，那些还没算出来的中间值全是空的，而拼「价格依据」那段文字没考虑这种情况，会直接抛异常。异常被日志吞掉，结果就是历史页上什么都不写，你只看到单没挂，一个字的解释都没有。悬崖否决走的是另一条分支，没受影响；这次的新闸门会踩到，一并修了。

**预演页的规则标签会自相矛盾。** 悬崖或断层上限否决时，市场还没归级，标签兜底写的是「无区间档」，可这两种情况奖励区间里明明是有买档的，跟同一行显示的「最大断层 15¢」对不上。现在统一显示「未归级」，真实原因看下面那行跳过说明。

---

### 升级说明

不需要改任何配置。想用新功能就去配置页填「断层上限」，想保持原样就别管它。
