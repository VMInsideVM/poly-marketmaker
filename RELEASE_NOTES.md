## v6.0.1 · 挂单快很多

这版只干一件事：把「开程序」到「第一单挂出去」的等待时间大幅压短。策略、挂单价格、离场逻辑一个字都没改，挂的还是原来那些单，只是不用再干等了。

### 以前慢在哪

有用户反馈开机一个多小时才挂上第一单。翻了他的日志，启动到首单 64 分钟：25 分钟在核对各市场的精确奖励，38 分钟在抓订单簿。

抓簿那 38 分钟里，程序把候选池 1900 多个市场的簿全抓了一遍，可最后真正能下单的只有一百来个。多抓的那些，簿拿回来当场就扔。而且每抓一个簿还要额外发一次「问价差」的请求，那个请求比抓簿本身还慢三倍，可价差不过是卖一减买一，本来自己就能算。

### 改了什么

价差自己算，不再多发那次请求。抓簿的网络请求直接少一半。

先筛再抓簿。先用不需要订单簿就能判的条件（品类、奖励、结算日、冷却、档位、单价区间）把候选池过一遍，只给真正可能下单的那些市场抓簿。多个钱包共用的市场只抓一次。

精确奖励也只查该查的。只对档位和结算窗口对得上的市场去核对，其余的用批量接口给的数值兜底，不再为它们发请求。

三条叠起来，首单等待大概能压到原来的六分之一。具体快多少取决于你启用了几个档位，档位匹配是最狠的那道筛子。之后每轮更新挂单的间隔也会跟着缩短。

### 要注意的

- 挂单行为完全不变，还是原来那些市场、那个价格、那个份数。这版纯粹是把白跑的网络请求砍掉。
- 「市场发现」页上，档位或结算窗口对不上的市场，奖励数字现在显示的是批量接口的估值，价差显示「—」。这些市场本来就不会下单，不影响任何决策。
- 本地数据库、钱包、模板、档位配置全部原样沿用，静默安装、自动重启。

### 顺带修了一个 bug

绑了多个模板的用户，如果两个模板的结算窗口或启用档位不同，采集阶段可能把它们当成同一个来处理，导致其中一个想要的市场被漏掉。现在按各自的窗口和档位分别处理。
