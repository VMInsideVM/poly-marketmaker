## v3.2.0 · 品类计数本地化 + 市场发现按品类看 + 扫描体验一整轮打磨

这一版都围绕「配置页的品类」和「市场发现的扫描」,把之前几处别扭的地方一起理顺了。

### 品类计数存本地,配置页秒开

以前每次进「配置」页,品类那一栏都要联网现算各品类的市场数,冷启动或隔久了要转好几秒。现在计数**存在本地**,打开立刻用上次的结果、跨重启也不丢;计数在每次「扫描市场」后自动更新,也可以点品类栏旁边的「刷新计数」手动拉。另外**不再隐藏 0 个市场的品类**:所有品类都列出来,计数只是参考。

### 市场发现:按品类看

「市场发现」页顶部多了一排**品类选项卡**(`全部 / 政治 / 经济 / …… / 其他`,各带数量),点一个只看那一类;同时属于多个品类的市场归到主品类,行里用小字标出它还属于哪些品类。

### 扫描:即时反馈 + 全局进度 + 确认重扫

- **点了立刻有反应**:一按「扫描市场」就清空列表、显示醒目的「扫描中」横幅,不再干等、也不再残留上一批市场。
- **进度全局可见**:扫描是后台在跑,和你停在哪个页面无关。现在侧边栏有一个「⟳ 扫描中 47/120」的指示,**切到任何页面都看得到**,切回市场发现页也接着显示真实进度(不是从头重来)。
- **扫描中再点扫描会先问你**:弹确认「中断当前扫描、用最新配置重来?」,确认才用最新配置重扫,取消则当前这轮继续。防误触双开,又能在改了配置后立刻重扫。

### 顺带修的

- **梯队预演偶发「预演失败」**:接口在某些情况下抛异常时返回的是 HTML 报错页,前端解析不了。已改为始终返回明确的错误信息;并修掉一个真正的取值错误(内存里的市场数据没有顶层 token 字段导致 `KeyError`),现在预演能正常出 YES/NO 两侧梯队。

### 升级与兼容

静默安装、自动重启;本地数据库和已有模板原样沿用。所有改动都向后兼容,更新后按上面的说明用即可。
