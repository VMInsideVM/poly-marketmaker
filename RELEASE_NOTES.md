## v3.3.0 · 新增「网关式单档」挂单模式 + 浮盈市价止盈 + 止损可关

这一版加了一整套全新的、**可选**的做市玩法。默认什么都没变——不动配置的话,老的多档做市和挂卖一止盈跟以前一模一样,现有钱包零影响。想用新玩法,去配置页切一下就行。

### 新挂单模式:网关式单档

「配置」→「策略参数」多了个**挂单模式**开关,默认「多档做市」(老行为),可以切成「网关式单档」。切过去之后,选中它的模板按这套逻辑走:

- 只看**奖励区间内**的买档,按「相邻两档最大价差」把市场分三级:>10¢、5–10¢、<5¢(三个阈值都能配)。
- 价差 >10¢ 的市场,再查一道「断层上方各档风险系数之和 ≥ 门槛」的闸——不够就整个市场不做。
- 通过后,从买一往下取**第一个风险系数 > x** 的那一档,**每个市场只挂一单**(挂最低份数)。系数门槛 x、断层阈值、风险系数用的金额数值表,都在配置页里改。

一句话:稀疏、有断层的盘口它会更挑剔甚至跳过,密的盘口正常挂;每个市场就一单,不铺档。

### 止盈方式可选「浮盈市价卖」

同一页多了个**止盈方式**开关:

- 「挂卖一做 maker」(默认):跟以前一样,浮盈时挂在卖一慢慢吃价差。
- 「浮盈市价卖」:成本一旦低于买一(也就是浮盈了),**立刻市价卖出兑现**;没浮盈(保本/套牢)就挂在成本价等回本。适合想落袋为安、不惦记那点价差的打法。

### 止损可以关了

「止损方式」新增「**关闭止损**」一项。选了它,套牢的仓不再触发强平,一直挂成本价等回本。

⚠️ 关掉之后套牢仓就没有任何兜底了,极端行情理论上能扛到归零——这跟系统一直以来的强平保护是反着来的,想清楚了再关。(浮盈市价卖本身是安全的,只在买一高于成本时才动手,成交一定不低于成本。)

### 顺带

- 修了个挂单价分级的浮点小坑:像 `0.28−0.18` 这种,计算机里会算成 `10.000000000000002`,会把「恰好 10¢」误判成「大于 10¢」。现在按分四舍五入去掉这点误差,边界判得准了。
- 「市场发现」页的梯队预演:如果当前模板是「网关式单档」,会明说这个模式每市场只挂一单、预演仅供参考,不再拿多档的表格误导你。

### 升级与兼容

静默安装、自动重启;本地数据库和已有模板原样沿用。所有改动都向后兼容,更新后按上面的说明用即可。
