## v6.0.0 · 挂单份数自己定（档位模块）+ 资产曲线

这版两件事：挂单参数从「全局一套」改成「按市场最低奖励份额分档配置」，每档可以自己定挂多少份；另外加了一页账户净值曲线。因为改了下单行为，算主版本。

### ⚠️ 升级后必须先配档位，否则不挂单

这是刻意的设计，不是 bug：

- 升级后档位模块列表是**空的**，引擎**一单都不会挂**，直到你去「配置 → 档位模块」为每个模板添加档位。仪表盘顶部会一直提示还有哪些模板没配。
- 首轮下单时，**现有的在挂买单会被全部撤掉**（因为那些市场此刻还匹配不上任何档位）。持仓和卖单不受影响，止损照常守着。配好档位后，符合条件的市场会自动重新挂上。

### 变了什么

**新增：档位模块（按最低奖励份额精确匹配）**

Polymarket 每个奖励市场都规定了一个最低挂单份数（20、50、100……），这个数字直接决定了做这个市场的最小本钱和风险，所以挂单参数不该是全局一套。现在改成按档配：

- 一个模块 = 一个档位值 + 这一档专属的**挂单份数**、三级选档门槛、高位系数和门槛、金额数值表。
- **挂单份数可以大于档位值**：档位 20、份数填 40，就是在最低份额 20 的市场上挂 40 份。这是本版最想要的能力——以前只能死板地挂市场最低份数。
- **精确匹配**：市场的最低份额必须恰好等于某个已启用模块的档位值才会做。启用 20 档和 50 档，最低份额 30 的市场就完全不碰。
- 停用某档 = 不再做这一档的市场，下一轮它上面的买单会被撤掉（持仓照常卖出）。

配置页的「奖励最低份额范围」「规则1/2/3 选档系数门槛」「规则1 高位系数和门槛」「金额数值表」四项已经取消，它们都下沉到每个档位模块里了。宽/中断层阈值、悬崖探测仍是模板全局。

**新增：资产曲线**

引擎运行期间，每个钱包每天记一次净值（现金 + 持仓市值，引擎启动时也记一次）。左侧新增「资产曲线」页，按钱包画净值日线，另有一条现金辅线，方便看出钱是变成了仓位还是真的赚了/亏了；上方日期框可查任意某天的净值明细。持仓市值只用于这里展示，不参与任何交易决策。

**使用说明同步更新**

「使用说明」页补上了档位模块怎么配、资产曲线怎么看，参数表也和现在的配置页对上了。

### 升级与兼容

- 本地数据库、钱包、已有模板原样沿用，静默安装、自动重启。旧的全局挂单参数会在启动时自动清理。
- 配档位前引擎不挂单（见上方提示）。建议先配一两个你实际在做的档位（比如 20、50），跑一轮看看「市场发现」页的预演结果符不符合预期，再加其它档。
