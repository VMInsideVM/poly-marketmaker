## v7.1.0 · 代理支持 SOCKS5，连不通的代理不再被存进去

这版只动代理这一块，做市、下单、离场的行为和 v7.0.0 完全一样。

### 现在 SOCKS5 代理也能用了

以前只支持 HTTP 代理。不少供应商给的机房 IP 其实只开 SOCKS5（比如 iproyal），账户密码看起来一模一样，填进去却怎么都连不上，界面上也看不出是为什么。

填法没变，还是 `host:port:账户:密码`。保存的时候程序自己判断该走 HTTP 还是 SOCKS5，不用你操心。

### 保存前会先测一遍

点保存后，程序拿这个代理实际去连一次 Polymarket：

- **连通** → 保存，并告诉你走的是哪种协议、出口 IP 是多少。拿这个 IP 可以核对每个钱包是不是真的走了各自的线路。
- **连不通** → 不保存，当场报错。程序在任何情况下都不会绕开代理用你的真实 IP 联网，所以一个连不通的代理要是被存下来，这个钱包就一直挂不出单，还不告诉你原因。宁可存不进去。

代理列表里会标出哪些是 SOCKS5，一眼能分清。

### 老代理不受影响

已经配好的 HTTP 代理原样能用，不用重填。

### 从源码运行的话

装一下新加的两个依赖：`pip install -r requirements.txt`。用安装包或 DMG 的不用管，已经打进去了。
