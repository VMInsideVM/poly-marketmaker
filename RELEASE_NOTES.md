## v6.0.2 · 日志按天分文件

程序的运行日志以前全堆在一个文件里，一直往下写、从不分割。有用户的日志攒到了 3.1 GB，出问题想翻记录，光打开就得等半天。现在改成每天一个。

### 变了什么

当天的日志还是叫 `market_maker.log`，位置也没变。过了午夜，前一天的内容会自动改名成 `market_maker.log.2026-07-15` 这样带日期的文件，新的一天重新开一个。程序不用一直开着，隔几天再启动，之前的也会正确分好。

只保留最近 30 天，更早的自动删掉。要找哪天的记录，直接打开那天的文件，不用在一个几 GB 的大文件里翻。

### 升级后

你原来那个大日志文件会被整个改名保留下来，然后跟着 30 天的窗口自然清掉，不会立刻消失。数据库、钱包、模板和档位配置都不受影响。

这版只动日志，交易行为一个字没改。
