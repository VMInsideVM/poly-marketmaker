## v3.4.0 · 网关式单档：选档系数门槛拆成三档（规则1/2/3 各一个）

上一版的「网关式单档」模式里，选档只有一个统一的「风险系数门槛 x」：不管市场被判成规则几，都拿同一个门槛卡。这版把它拆成三个，规则1、规则2、规则3 各配一个，可以按盘口的稀密程度分别定松紧。

### 变了什么

配置页「挂单模式」下，原来那一个「选档风险系数门槛 x」输入框，换成了三个：

- **规则1 选档系数门槛**（宽断层，相邻档最大价差 > 宽断层阈值）
- **规则2 选档系数门槛**（中断层，价差在中~宽之间）
- **规则3 选档系数门槛**（密盘，价差 < 中断层阈值）

市场先按「奖励区间内相邻档最大价差」归到某一级，选档时就只用那一级的门槛，从买一往下取第一个「风险系数 > 该门槛」的档挂一单。比如密盘（规则3）想挂得松些、门槛设 0；有断层的稀盘（规则1/2）想挑厚档、门槛调高。

规则1 那道「断层上方各档风险系数之和 ≥ 门槛」的市场闸没动，还是先过闸再选档。

### 升级与兼容

只影响「网关式单档」模式，默认的「多档做市」完全不受影响。三个新门槛默认都是 0，跟上一版 x=0 的效果一样，不去动它就跟 v3.3.0 完全一致。

如果你在 v3.3.0 给这个门槛设过非 0 的值：升级后它会回到默认 0，需要到配置页按规则重新填三个门槛。

静默安装、自动重启，本地数据库和已有模板原样沿用。
