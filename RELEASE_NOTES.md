## v9.1.1 · 下单轮稳定性与可观测性修复

### 修复

- Gamma UMA 结算状态查询按 50 个市场分批，避免候选市场较多时 URL 过长而持续返回 422。
- 单个 Gamma 批次失败时，其余批次仍正常生效；失败的批次继续采用 fail-open，不会误撤单或误跳过市场。

### 排查改进

- 每个钱包每轮下单都会记录 `[place]` 汇总，列出候选数、实际挂单数，以及因冷却、余额、预算、并发上限、盘口、黑名单和交易所拒单而跳过的数量。
- 当出现“挂单数不补”时，可直接从该汇总判断具体原因。

### 升级说明

无需修改配置或数据库。建议升级后观察日志中的 `[place]` 行，确认各钱包的补单状态。
