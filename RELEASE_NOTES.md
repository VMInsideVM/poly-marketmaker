## v2.2.0 · 新增「使用说明」页 + 离场两段式 + 多档厚度规则改为「大于」

### 新增：应用内「使用说明」页

左侧菜单多了一页「使用说明」，把整套程序怎么跑、每个参数是什么意思都讲清楚了，包括运行逻辑、策略参数、引擎参数和离场逻辑。看不懂的参数保持默认即可，默认值是按经验调好的。

### 离场逻辑改为两段式

卖出持仓的判断简化成两种情况。成本低于买一价（处于盈利）时，直接按成本价挂卖单：成本比买一低，这笔单会立即按买一价（高于成本）成交，等于马上锁定利润离场。成本不低于买一价（保本或套牢）时，挂在卖一价被动等回本，只有当亏损（成本减买一）达到「强平阈值」才市价清仓止损。

「强平阈值 θ_stop」默认 5 美分，在「配置 → 策略参数」里按模板单独设置，是现在唯一的止损设置。原来的「浮亏阈值 θ_loss」和「成本≤买一时离场方式」两个设置已经去掉，不再使用。

### 多档厚度规则由「小于」改为「大于」

多档挂单规则里，每个区间的厚度条件从「厚度 < X」改成「厚度 > X」：累计厚度超过 X 才用这个区间的动作。

同一档里如果配了多个区间，阈值要从大到小填（例如 厚度>10、厚度>5、其余），否则小阈值会把大的挡住。只用默认规则（其余 → 最小份数）的用户不受影响。

### 升级与兼容

静默安装、自动重启；本地数据库和已有模板原样沿用。离场行为有变化，更新后请留意卖单挂价是否符合预期。
