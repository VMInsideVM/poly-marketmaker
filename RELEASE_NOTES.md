## v2.4.0 · 并发错读止损修复（重要）+ 做市区间变量可选「风险系数」

### 重要修复：多钱包并发下偶发「按错止损线砸仓」

如果你同时跑多个钱包，之前存在一个严重问题：程序里所有线程共用同一个数据库连接、且没有加锁，高并发的瞬间会偶发读到错误的模板配置——把你设置的止损比例误读成默认的 20%，于是在手持仓被按错误的止损线市价清仓。曾出现过「设了 80% 止损，持仓却在亏损约 50% 时被市价砸掉」的情况，根因就在这里（同一持仓相隔几秒的两条离场日志，止损线在 80% 和 20% 之间跳变）。

现在改成每个线程各用一条独立数据库连接 + WAL 模式，彻底消除这个竞态。**强烈建议所有用户，尤其是多钱包用户，尽快更新。**

### 新增：做市「区间变量」可选——累计厚度 / 风险系数

多档挂单规则里每档的阈值，现在可以选按哪个变量匹配：

- **累计厚度**（默认，行为不变）：买一往下累计的订单厚度。
- **风险系数**（新）：`本档盘口深度 ÷ 最低份数 ÷ 金额数值`。金额数值按价格档在一张可编辑的表里配（例：10–20¢→1、20–25¢→1.5、25–30¢→2），价格超出表的档不挂。

配套还加了：**奖励最低份额范围筛选**（下限/上限，可只做特定份额档的市场）；配置页的区间变量下拉、金额数值表编辑器、份额范围字段；档规则的标签会随所选变量自动切换（累计厚度 ↔ 风险系数）；市场发现的梯队预演表在风险系数模式下多显示一列风险系数。

一切皆配置——具体策略只是一组参数，系统里不写死。默认仍是累计厚度模式，老模板与行为原样不变。

### 升级与兼容

静默安装、自动重启；本地数据库和已有模板原样沿用。所有新增字段都有默认值，更新后行为不变，想用新功能到「配置 → 策略参数」里开启即可。
