## v2.3.0 · 可配置止损（按比例/按固定金额）+ 强平成交价修复 + 卖单硬底线

### 新增：止损可自定义

止损线现在可以在「配置 → 策略参数」里按自己的需求设，两种方式二选一：

- **按比例**（默认）：亏损达到成本的百分之多少就清仓，默认 20%。成本越高，允许亏的绝对金额越大；成本越低，止损越紧。例：成本 0.14、按 20%，跌到 0.112 触发。
- **按固定金额**：亏损达到固定的几美分就清仓（和成本无关，原来的方式，默认 5 美分）。

选「按比例」时只显示比例输入框，选「按固定金额」时只显示美分输入框。默认从原来的固定 5 美分改成了按比例 20%；已有模板会自动用上新默认，想保持固定金额的话在配置页切回去即可。

### 修复：强平成交价显示（曾把亏损显示成盈利）

触发兜底强平时，记录里的卖出价以前用的是 Data API 的「现价」，在盘口很薄的市场里常和真实成交价差很远，有一笔实际亏 6.5 美分的单子界面却显示成交价 0.13、看着像赚的。现在改成记真实成交价（优先取市价单返回的成交额折算，取不到就用盘口买一价），和你在 Polymarket 上看到的对得上。

### 新增：卖单硬底线，永不低于成本

给离场卖单加了一道硬底线：下单前价格强制不低于成本价，即便盘口出现异常报价（比如买一是个滞后的幻影价、卖一比买一还低）也绝不会把卖单挂在成本下方。盈利侧原本直接挂卖一，现在也对成本取下限。同时把每次离场的盘口（买一、卖一）、成本和止损线都打进日志，方便排查个别异常成交。

### 升级与兼容

静默安装、自动重启；本地数据库和已有模板原样沿用。默认止损方式变为按比例 20%，更新后留意一下，想用固定美分就去配置页切回。
