# Polymarket 做市策略（定稿 v4）

> 本版为正式定稿。相对 v3：参数与口径均已拍板，**删除**「夜间模式」章节、「剩余待确认」章节以及两个历史附录（安全距离 / v2 离场表），正文章节据此重排。
> 价差 / 价格单位为美分（¢），份数单位为 share。
> 凡标注「（模板参数）」的数值均可在 §9 的挂单模板中按钱包单独配置。

---

## 一、名词定义

| 名词 | 定义 |
| --- | --- |
| **最低份数** | = 最低份额 = 最低要求数量 = 奖励要求最小份数，四者同一，单位 = share |
| **单份奖励** | 每日 LP 总奖励 ÷ 最低份数 |
| **订单厚度** | 某价格上的买单份数 ÷ 最低份数 |
| **累加厚度** | 从高价往低价，逐个有效价格累加的订单厚度之和 |
| **奖励范围** | 直接读取该市场配置（不自行推算） |
| **有效价格** | 订单簿上、处于奖励范围内、且买单厚度 ≥ 1 的价格 |
| **买一 / 卖一** | 当前订单簿最高买价 / 最低卖价 |
| **盘口价差** | 卖一 − 买一（¢，≥0） |
| **一侧** | 指 YES 或 NO 其中一边；「一侧最多 N 单」= YES ≤ N 单 **且** NO ≤ N 单 |
| **挂单模板** | 一组完整做市参数的命名集合（筛选门槛、累加厚度阈值、各档份额动作、离场参数、敞口 / 并发上限等）。导入钱包时为其指定模板，钱包据此独立运行（见 §9） |

---

## 二、全局参数与约定

- **比较符**：§5 各价格档的累加厚度**区间边界**（< / ≤ / > / ≥）按档独立配置，只需保证区间无缝覆盖（见 §5.1）；§3 单份奖励用 **≥**。
- **价格最小变动**：1¢ tick，所有挂单价格对齐到整美分。
- **挂单价格**：选定后**就挂在该有效价格上**，不上浮 / 下移。
- **每单数量（份数）**：由各档规则表中、该累加厚度区间所选的**份额动作**决定（§5.0 列出五种：固定份数 / 固定金额 / 最小份数 / 钱包总额 / 不挂），**全天统一生效**；不再固定为「最低份数 + 1」。
- **双边报价**：YES、NO 两侧各自独立运行同一套逻辑，**始终尝试双边报价**。
- **最低份数取档（关键）**：阈值表只有 20 / 50 / 100 / 200 / 250 五档，实际最低份数按 **向上取档（更保守）** 映射：

  | 实际最低份数 s | 取档 |
  | --- | --- |
  | s ≤ 20 | 20 |
  | 20 < s ≤ 50 | 50 |
  | 50 < s ≤ 100 | 100 |
  | 100 < s ≤ 200 | 200 |
  | 200 < s ≤ 250 | 250 |

- **资金 / 仓位**：总投入资金不限制；**单市场最大敞口 = ≤ 250U 且 ≤ 500 share**（两上限同时生效；模板参数）；最多并发做市市场数为可配置阈值（模板参数）。所有份额动作算出的份数都**受单市场最大敞口约束**（取不超过上限的值）。

---

## 三、市场筛选 / 激活条件

同时满足以下条件才纳入做市（**单份奖励 = 每日 LP 总奖励 ÷ 最低份数**）：

| 条件 | 取值 |
| --- | --- |
| 最低份额 | 0 < 最低份额 ≤ 250（share） |
| 每日 LP 奖励 | ≥ 6U（模板参数） |
| 价差（= 奖励最大允许价差 max spread） | 1 ≤ 价差 ≤ 6（¢，模板参数） |
| **单份奖励** | **≥ 该取档对应的单份奖励阈值**（按取档分设，见下表） |
| 排除市场 | 电竞、体育、天气 一律排除 |

**单份奖励阈值（按最低份数取档分设 · 模板参数）**

| | 20 | 50 | 100 | 200 | 250 |
| --- | --- | --- | --- | --- | --- |
| 单份奖励阈值 | 0.30 | 0.30 | 0.30 | 0.30 | 0.30 |

> 判断时先按 §2 取档（向上取档），再用**该档阈值**与市场实际单份奖励比较：**实际 ≥ 该档阈值 → 通过**。五档默认均 0.30，可各自单独调整（如高份额档放宽、低份额档收紧）。
> 注：取档（20/50/100/200/250）**仅服务于本节市场筛选**，不参与挂单与离场逻辑。

---

## 四、主流程

1. **查奖励是否激活**：未激活 → 加入观察名单（§6）。
2. **执行挂单逻辑**：YES、NO 两侧分别执行（§5）。
3. **持续维护**：4h 扫描 + 10min 复扫观察名单 + 实时监控（§6）。
4. **成交后离场**：按 §7 卖出；某侧成交后**仅暂停成交那一侧**（YES 或 NO）的新买单，直至**该侧**持仓平掉；**另一侧照常运行**。

> 注：单份奖励现在**仅用于 §3 市场筛选**（按取档分设阈值，见 §3 表）。挂单按 §5 的**多档挂单**模型——「档」= 从买一往下的价格档位，每档各带一张「累加厚度区间 → 份额」规则表，逐档判断在该档价格挂多少（或不挂）。

---

## 五、挂单规则（多档挂单：按价格档逐档判断）

> **「档」= 从买一往下数的价格档位（只数有效价格）**：档 1 = 买一；档 2、档 3…… 依次取买一**下方的有效价格**（§1 定义：在奖励范围内、买单厚度 ≥ 1 的价位），**跳过空价位**——某个 tick 若无买单或厚度 < 1，**不占用档位**，直接看下一个有效价格。共配置 **K** 个价格档（**K 默认 6、可自行增加**，见 §5.2；均须在奖励范围内）。含义：除买一外，各档都落在盘口**已有挂单**的价位上（在已有流动性上叠加），不在空价位单独造档。
> 每个价格档**独立配置一张「累加厚度区间 → 挂单份额」规则表**：用若干阈值把「到该档的累加厚度」切成区间，每个区间对应一种挂单动作（份额，或不挂）。
> 因为各档**各自绑定一个固定价格**，一侧最多 K 个买单分布在 K 个不同价格上，**天然不会同价重复挂单**。
>
> **关键澄清（阈值决定份额、不决定价格）**：每档的阈值只在**该档内部**用来选「挂多少份」（或不挂）——它是"累加厚度落在哪个区间"的分段点，**从不输出价格**。价格完全由「档」在盘口的位置唯一确定（档 1 = 买一，档 2 = 下一档……）。因此**阈值不会"解析出一个价格"**，自然也不存在「两档阈值解析到同一价格、在同价重复挂单」的情形——无论阈值取多少、各档区间怎么配，**档与价格始终一一对应**，K 个档对应 K 个互不相同的价位。

### 5.0 单个区间的挂单份额动作（每个区间选其一 · 模板参数）

- **固定份数**：直接挂 N 份（如示例的 50 份）。
- **固定金额**（金额可自定义）：份数 = ⌊金额(U) ÷ 该档价格⌋；若 < 最低份数则取最低份数。
- **最小份数**：= 最低份数（奖励要求最小份数）。
- **钱包总额（全额）**：= 扣除同市场其他档后的剩余预算 ÷ 该档价格；本市场预算 = min(钱包可用余额, 单市场最大敞口)，各档之和始终 ≤ 单市场最大敞口（多档同选全额时，靠前的取走剩余、靠后的可能为 0）。
- **不挂**：该区间不下单。

> 所有份额结果向下取整，并整体受 §2 单市场最大敞口约束。

### 5.1 逐档判断流程

对 档 1 … 档 K，**逐档**执行：

1. 算出**到该档的累加厚度** = 从买一累加到该档价格的订单厚度之和（订单厚度 = 该价位买单份数 ÷ 最低份数）。
2. 看它落在**该档规则表**的哪个累加厚度区间。
3. 在**该档对应的价格**挂该区间指定的份额（或不挂）。

各档相互独立、各挂各价；不存在两档抢同一价的问题。

**示例（格式参考）**

| 价格档 | 阈值 | 规则（按到该档的累加厚度） |
| --- | --- | --- |
| 档 1（买一） | 5 | 累加厚度 < 5 → 挂 **50 份**；≥ 5 → 挂**最小份数** |
| 档 2 | 10 | 累加厚度 < 10 → 挂**钱包总额**；> 10 → 挂**最小份数** |
| 档 3（多阈值示例） | 5、15 | < 5 → 挂**钱包总额**；5 ≤ 厚度 ≤ 15 → 挂 **50 份**；> 15 → **不挂** |

> 每档可设**多个阈值 → 多个区间**（不止一个），每个区间独立配份额（固定份数 / 固定金额 / 最小份数 / 钱包总额 / 不挂，见 §5.0）；档 1、档 2 各用 1 个阈值（2 个区间），档 3 用 2 个阈值（3 个区间）作对照——可见**阈值数量、区间份额都是"档内部"的事，与价格无关**。
> **边界须无缝覆盖**：每档把区间写成不漏不叠（如 `[0,5)` 与 `[5,∞)`），避免某个累加厚度值落不进任何区间。各档的边界（< / ≤ / > / ≥）可独立配置。
> 上表仅作**格式参考、不是默认值**；K、各档区间与份额均为模板参数（**K 默认 6、规则表默认留空待用户填**，见 §5.2）。

### 5.2 默认价格档数与规则表（规则表待用户填）

> **默认价格档数 K = 6**（非硬上限：可在模板里继续往下加 档 7、档 8…… 逐档照配即可；也可设更少）。
> **每档规则表默认留空**——本版**不预置任何基线数值**，每档的「累加厚度区间 → 份额动作」由用户在所选模板中**自行填写**。下表给出 6 档空骨架，填写时遵循 §5.0（份额动作五选一：固定份数 / 固定金额 / 最小份数 / 钱包总额 / 不挂）与 §5.1（区间须无缝覆盖、边界 < / ≤ / > / ≥ 按档独立配）。

| 价格档 | 阈值（累加厚度分段点） | 规则（按到该档的累加厚度 ct） |
| --- | --- | --- |
| 档 1（买一） | （待填） | （待填） |
| 档 2 | （待填） | （待填） |
| 档 3 | （待填） | （待填） |
| 档 4 | （待填） | （待填） |
| 档 5 | （待填） | （待填） |
| 档 6 | （待填） | （待填） |

> **填写要点**：每档可设任意个阈值 → 任意个区间，每区间独立选一种份额动作；务必无缝覆盖（如 `[0,a)`、`[a,b)`、`[b,∞)`），别让某个 ct 值落空。
> **口径**：累加厚度按**当前盘口快照**计算（含自己已在场的旧单），同一轮判断各档共用同一份快照（与 §6「撤改收敛」一致）。

---

## 六、监控与撤单（三档节奏）

| 节奏 | 职责 |
| --- | --- |
| **每 4 小时** | 扫描发现新市场 + 对全部在做市场**全量重判** |
| **每 10 分钟** | 复扫**观察名单**：仍满足筛选条件 → 进入挂单判断；不再满足最初筛选条件 → 移出观察名单 |
| **实时** | 看守已有挂单；一旦挂单条件不再满足，**立即撤销**该市场对应挂单，并**重新进入判断** |

> **撤改收敛原则**：每轮重判后，仅当某挂单的**目标价或目标份数发生变化**时才撤旧挂新；**目标没变就保持不动**，避免无谓的频繁撤挂。买单（§5 多档）与卖单（§7 离场）皆适用。

---

## 七、成交后处理 / 离场逻辑

**核心目标：尽快把持仓卖出**，不让它占用资金、影响后续挂单。某侧成交后**仅停止该侧**新买单，直至该侧持仓平掉（另一侧照常）。

**成本价（加权平均）**：按各笔成交的份数加权。例：20¢ 成交 30 份、25¢ 成交 40 份 → 成本 = (0.20×30 + 0.25×40) ÷ (30 + 40)。与份数 / 金额的绝对值无关，只看加权单价。

令 **买一** = 当前最高买价、**卖一** = 当前最低卖价。每轮监控按下列判断执行（**未卖完则下轮继续此循环，直到平仓**）：

**A. 成本 ≤ 买一**（可在买一价无损卖出，持平或浮盈）
- 在「**挂卖一价**」与「**市价卖出**」之间二选一（模板参数 / 开关）：
  - 挂卖一：摆在卖一等更好的价（maker，吃满价差）。
  - 市价：贴着买一立即清掉。

**B. 成本 > 买一**（浮亏），令 **亏损 = 成本 − 买一**（¢）、**亏损阈值 = θ_loss**、**最低卖出价 = 成本 − θ_loss**、**强平阈值 = θ_stop**（**要求 θ_stop > θ_loss**）：

- **B0 最终兜底（强平）**：若 **亏损 ≥ θ_stop** → **市价清仓全部仓位**（不设价格下限、接受坏价；下轮未清完继续清），**不再走下面的逐级扫单**。
- **否则（亏损 < θ_stop）走逐级扫单**：
  1. **逐级扫单到「最低卖出价」为止**：下一个以「最低卖出价」为限价的市价单（marketable limit / FAK），从买一往下依次吃掉**所有 ≥ 最低卖出价**的买单流动性。
  2. **扫不动的剩余 → 挂卖一价**：扫到「最低卖出价」后仍有剩余仓位（再往下已跌破亏损阈值，不再吃），把剩余仓位以**卖一价**挂限价单（GTC）。
  3. 下一轮重新评估（买一可能回升 → 继续扫；亏损升至 ≥ θ_stop → 转 B0 强平；成本 ≤ 买一 → 转入 A）。

> 三段式：亏损 ≤ θ_loss → 扫单吃到最低卖出价、剩余挂卖一；θ_loss < 亏损 < θ_stop → 无可扫流动性、整仓挂卖一等回升；**亏损 ≥ θ_stop → 市价清仓止损**。θ_stop 即"绝对止损线"，堵住了"挂卖一无限扛单"的尾部风险。

> **图解**（亏损阈值 = 5、成本 = 15 → 最低卖出价 = 10）：
> - 买一 = 10（亏损恰为 5）：挂 10¢ 卖单吃掉买一处流动性；不够平仓的，剩余挂卖一。
> - 买一 = 13：先吃 13，再吃 12、11、10；若 13→10 全吃完仍有剩余，**绝不**再吃 9（已破阈值），剩余挂卖一。
> - 买一 < 10（一开始就破阈值）：无可吃流动性，整仓**直接挂卖一**。
>
> 所以 B 分支**不是**简单市价清仓，而是「**一级一级扫到你的最低卖出价，扫不动的转挂卖一**」。因为是限价在「最低卖出价」上扫，**绝不会成交在该价以下**——盘口瞬时假跌也卖不穿（自带防错杀）。

> 亏损阈值 θ_loss 与强平阈值 θ_stop 均为**全局单值**（模板参数，按 ¢ 计、对全市场统一，**不按取档 / 最低份数分设**），仅需保证 **θ_stop > θ_loss**。逐级扫单因限价在「最低卖出价」上，**绝不成交在该价以下**（自带防错杀）；唯一例外是 **B0 强平**为真·市价、可能成交在很低价位——这是兜底止损的代价，刻意为之。
> 持仓平掉后，该市场恢复正常的挂买流程。

---

## 八、价格下限与双边要求

- 若某侧的**挂单价格 < 10¢**，则要求 YES、NO **两侧都有挂单**；若无法满足 → 该市场不挂单。
- 该要求全天生效。

---

## 九、挂单模板与钱包导入

**挂单模板**是一组完整做市参数的命名集合。系统可保存多个模板（如「保守」「激进」等）。**导入钱包时为该钱包选定一个模板**，钱包即按该模板的全部参数独立运行；不同钱包可绑定不同模板**并行**做市。

**模板包含的参数（均可按模板单独取值）**：

| 分类 | 参数 |
| --- | --- |
| 份额动作（§5.0） | 每个累加厚度区间选一种：固定份数 / 固定金额 / 最小份数 / 钱包总额 / 不挂；固定份数的份数、固定金额的金额（**按档 × 区间分别设**） |
| 市场筛选（§3） | 每日 LP 奖励下限（默认 6U）、价差范围（默认 1–6¢）、**单份奖励阈值（按取档 5 档分设，默认 0.30）** |
| 多档结构（§5） | 价格档数 K（**默认 6、可增减**）；每档的「累加厚度区间 → 份额动作」规则表（**默认留空待填**，见 §5.2） |
| 离场（§7） | 成本 ≤ 买一时的动作（挂卖一 / 市价）、**亏损阈值 θ_loss**（决定最低卖出价）、**强平阈值 θ_stop**（亏损 ≥ 它即市价清仓；θ_stop > θ_loss） |
| 资金 / 并发（§2） | 单市场最大敞口（默认 ≤ 250U 且 ≤ 500 share，两上限同时生效）、最大并发市场数 |
| 价格下限（§8） | <10¢ 双边要求（一般固定开启） |

**导入钱包流程**：

1. 导入钱包（地址 / 私钥 / API 凭证）。
2. 为该钱包**选择一个挂单模板**。
3. 钱包按所选模板的全部参数独立运行做市与离场。
4. 可随时为钱包**切换模板**：切换后于下一轮 4h / 10min 扫描或实时重判时生效；建议切换前先撤掉该钱包在场旧单，避免新旧参数混挂。
